# ===========================================
# AWAD Email Backend - Environment Variables
# ===========================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!

# ===========================================
# Server Configuration
# ===========================================
SERVER_PORT=8080

# ===========================================
# MongoDB Configuration
# ===========================================
# Local MongoDB
MONGODB_URI=mongodb://localhost:27017/awad_email

# MongoDB Atlas (Cloud) - Example
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/awad_email?retryWrites=true&w=majority

# ===========================================
# JWT Configuration
# ===========================================
# IMPORTANT: Generate a strong random secret key (at least 256 bits / 32 characters)
# You can generate one using: openssl rand -base64 32
JWT_SECRET=YourVeryLongSecretKeyForJWTTokenGenerationThatShouldBeAtLeast256BitsLongForHS256Algorithm

# JWT Token Expiration Times (in milliseconds)
# Access Token: 15 minutes (900000 ms)
JWT_ACCESS_TOKEN_EXPIRATION=900000

# Refresh Token: 7 days (604800000 ms)
JWT_REFRESH_TOKEN_EXPIRATION=604800000

# ===========================================
# Google OAuth 2.0 Configuration
# ===========================================
# Get your Google OAuth credentials from:
# https://console.cloud.google.com/apis/credentials
# 
# Steps to get Google Client ID:
# 1. Go to Google Cloud Console
# 2. Create a new project or select existing one
# 3. Enable Google+ API
# 4. Go to Credentials > Create Credentials > OAuth 2.0 Client ID
# 5. Configure OAuth consent screen
# 6. Add authorized redirect URIs:
#    - http://localhost:3000 (for local frontend)
#    - http://localhost:8080/api/auth/google/callback (if needed)
# 7. Copy the Client ID

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ===========================================
# Logging Configuration
# ===========================================
# Log levels: TRACE, DEBUG, INFO, WARN, ERROR, OFF
# Development: DEBUG or INFO
# Production: WARN or ERROR

# Application log level
LOG_LEVEL=INFO

# Spring Security log level
LOG_LEVEL_SECURITY=WARN

# Spring Web log level
LOG_LEVEL_WEB=INFO

# ===========================================
# CORS Configuration (Optional - handled in code)
# ===========================================
# If you want to make CORS origins configurable:
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174

# ===========================================
# Environment
# ===========================================
# Options: development, staging, production
ENVIRONMENT=development

